# Prevent directory listing
Options -Indexes

# Deny direct access to config.php
<Files "config.php">
    Require all denied
</Files>

# Only allow PHP files in this folder
<FilesMatch "\.php$">
    Require all granted
</FilesMatch>
